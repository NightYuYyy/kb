version: "3.8"

services:
  kb:
    build: .
    container_name: kb
    ports:
      - "8765:8765"
    volumes:
      - ./data:/app/data
    environment:
      - KB_AUTH_TOKEN=kb-secret-change-me
    restart: unless-stopped
